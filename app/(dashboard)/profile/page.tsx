import UpdateProfileForm from "@/src/features/profile/components/UpdateProfileForm/UpdateProfileForm"

export const metadata = {
  title: "KOSH | Update Profile",
};

const Profile = () => {
  return (
    <div>
      <UpdateProfileForm />
    </div>
  )
}

export default Profile
